import type { NextPage } from "next";
import Layout from "../components/layout";

const Home: NextPage = () => {
  return (
    <Layout>
      <h1 className="text-xl text-blue">Hi</h1>
    </Layout>
  );
};

export default Home;
